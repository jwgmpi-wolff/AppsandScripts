name: Build iPhone companion IPA

on:
  workflow_dispatch:
  push:
    paths:
      - "phonesyncusbc/ios-companion/**"
      - ".github/workflows/build-ios-companion.yml"

jobs:
  build:
    runs-on: macos-14
    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Generate Xcode project
        working-directory: phonesyncusbc/ios-companion
        run: xcodegen generate

      - name: Build unsigned iOS app
        working-directory: phonesyncusbc/ios-companion
        run: |
          xcodebuild \
            -project PhoneSyncCompanion.xcodeproj \
            -scheme PhoneSyncCompanion \
            -sdk iphoneos \
            -configuration Release \
            -derivedDataPath build \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            build

      - name: Package IPA
        run: |
          mkdir -p phonesyncusbc/ios-companion/ipa/Payload
          cp -R phonesyncusbc/ios-companion/build/Build/Products/Release-iphoneos/PhoneSyncCompanion.app phonesyncusbc/ios-companion/ipa/Payload/
          cd phonesyncusbc/ios-companion/ipa
          zip -qry ../PhoneSyncCompanion-unsigned.ipa Payload

      - name: Upload IPA artifact
        uses: actions/upload-artifact@v4
        with:
          name: PhoneSyncCompanion-unsigned-ipa
          path: phonesyncusbc/ios-companion/PhoneSyncCompanion-unsigned.ipa
          if-no-files-found: error
